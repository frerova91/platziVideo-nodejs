const os = require("os");

// console.log("CPU info", os.cpus());
// console.log("IP address", os.networkInterfaces().en0.map(i => i.address));
// console.log("Free memory", os.freemem());
// console.log("Type", os.type());
// console.log("SO version", os.release());
// console.log("Usr info", os.userInfo());

//con estos metodos podemos identificar por ejemplo caso de uso tenemos un video juego el cual debemos saber que tiene el hardware y sistema operativo del dispositio en el cual se va  instalar para determinar que calidad de graficos maximo que soporta y en base a eso tomar una decision.

//la mayoria de la utilidades de node  pueden utilizar asncronas, simepre usa un bloque try{}cath{} con un callback error frits
